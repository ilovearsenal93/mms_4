<%= form_for @skill, url: edit_admin_skill_path(@skill.id)  do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :description %>
      <%= f.text_field :description %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
<% end %>